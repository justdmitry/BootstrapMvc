<h2>Paging</h2>

@using (Bootstrap.BeginGridRow())
{
    using (Bootstrap.BeginGridCol(0, 7, 7, 7))
    {
<pre>
@@using (var pages = Bootstrap.BeginPaginator())
{
    @@pages.ItemPrevious().Disabled()
    @@pages.Item("1").Href("/page1").Active()
    @@pages.Item("2").Href("/page2")
    @@pages.Item("3").Href("/page3")
    @@pages.ItemNext().Href("/page4")
}
</pre>
    }
    using (Bootstrap.BeginGridCol(0, 5, 5, 5))
    {
        using (var pages = Bootstrap.BeginPaginator())
        {
            @pages.ItemPrevious().Disabled()
            @pages.Item("1").Href("/page1").Active()
            @pages.Item("2").Href("/page2")
            @pages.Item("3").Href("/page3")
            @pages.ItemNext().Href("/page4")
        }
    }
}

<h3>Paging generator</h3>

<p>Page buttons generated automatically (set current page and total pages count), you can customize buttons text and href values template.</p>
@using (Bootstrap.BeginGridRow())
{
    using (Bootstrap.BeginGridCol(0, 7, 7, 7))
    {
<pre>
@@Bootstrap.Paginator(PaginatorSize.Small)
    .Autogenerated(2, 5)
    .ButtonsText("Page {0}", "« Previous", "Next »")
    .HrefTemplate("/show?page={0}")
</pre>
    }
    using (Bootstrap.BeginGridCol(0, 5, 5, 5))
    {
        @Bootstrap.Paginator(PaginatorSize.Small).Autogenerated(2, 5).ButtonsText("Page {0}", "« Previous", "Next »").HrefTemplate("/show?page={0}")
    }
}


<p>Hide previous/next buttons with <code>HidePreviousNextButtons</code></p>
@using (Bootstrap.BeginGridRow())
{
    using (Bootstrap.BeginGridCol(0, 7, 7, 7))
    {
<pre>
@@Bootstrap.Paginator(PaginatorSize.Large)
    .Autogenerated(2, 5)
    .HidePreviousNextButtons()
</pre>
    }
    using (Bootstrap.BeginGridCol(0, 5, 5, 5))
    {
        @Bootstrap.Paginator(PaginatorSize.Large).Autogenerated(2, 5).HidePreviousNextButtons()
    }
}

<p>Show only few page buttons before/after current using <code>PageButtonsCount</code></p>
@using (Bootstrap.BeginGridRow())
{
    using (Bootstrap.BeginGridCol(0, 7, 7, 7))
    {
<pre>
@@Bootstrap.Paginator().Autogenerated(7, 20).PageButtonsCount(3, 5)
</pre>
    }
    using (Bootstrap.BeginGridCol(0, 5, 5, 5))
    {
        @Bootstrap.Paginator().Autogenerated(7, 20).PageButtonsCount(3, 5)
    }
}